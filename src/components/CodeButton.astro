---
interface Props {
    code: string;
}

const { code } = Astro.props;
---

<a
    class="bg-white dark:bg-background hover:opacity-80 hover:cursor-pointer transition px-6 py-2 dark:text-white text-sm rounded-lg"
    href={code}
    target="_blank"
>Código</a> 